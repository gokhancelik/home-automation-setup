apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: factory-lens-api
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: factory-lens-api
  filterTags:
    pattern: '^sha-[a-f0-9]+'
    extract: '$0'
  policy:
    alphabetical:
      order: asc
