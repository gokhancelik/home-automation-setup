apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: home-assistant
  namespace: home-automation
spec:
  chart:
    spec:
      chart: home-assistant
      version: 13.4.2  # or latest listed on GitHub
      sourceRef:
        kind: HelmRepository
        name: home-assistant
        namespace: flux-system
  interval: 5m
  values:
    persistence:
      config:
        enabled: true
        existingClaim: home-assistant-config
    ingress:
      enabled: true
      className: nginx
      hosts:
        - host: ha.gcelik.dev
          paths:
            - path: /
              pathType: Prefix
      tls:
        - hosts:
            - ha.gcelik.dev
          secretName: ha-tls
